<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body { font-family: system-ui, sans-serif; margin: 24px; }
    header, footer { margin-bottom: 16px; }
    ul { padding-left: 18px; }
    form input, form select { margin: 4px 0; display: block; }
    .link { margin-right: 12px; }
  </style>
</head>
<body>
  {{> header}}
  {{{body}}}
  {{> footer}}
</body>
</html>
